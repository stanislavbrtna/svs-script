function main {
	for(i = 1; i < 200; i++;){
		text = "";
		if (i % 3 == 0) {
			text = "Fizz ";
		}
		if (i % 5  == 0) {
			text = text + "Buzz";
		}
		if (text == "") {
			text = "" + i;
		}
		print(text);
	}
}
